using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterSpawner
{
   
   
   // public bool SpawnCharacter(GameObject characterPrefab, Tile tile, Vector3 rotation, Character.Team team)
   // {
   //    while (NumberOfEnnemies > 0)
   //    {
   //       int coordX = Random.Range(0, Column);
   //       int coordY = Random.Range(Row - (Row / MaxDistanceEnemiesSpawn), Row);
   //      
   //
   //       if (SpawnCharacter(CharacterAIPrefab, _tileManager.GetTile(coordX, coordY),_enemiesDirection, (Character.Team.Team2), ))
   //       {
   //          NumberOfEnnemies--;
   //       }
   //    }
   //
   //    if (tile.IsOccupied) {return false;}
   //    GameObject character = Instantiate(characterPrefab, tile.Position, Quaternion.identity);
   //    character.transform.Rotate(rotation);
   //    _characterCount++;
   //    character.name = "Character" + _characterCount;
   //    Character characterReverence = character.GetComponent<Character>();
   //    characterReverence.CurrentTile = tile;
   //    characterReverence.CurrentTeam = team;
   //    CharacterList.Add(characterReverence);
   //    tile.SetCharacter(characterReverence);
   //    return true;
   // }
}
